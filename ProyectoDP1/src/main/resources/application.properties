# General application settings
keyCode = 01a828fgh8d56f25ba6dddb73b0ei574675b7721e299127a69h57651d67d3230

# AWS S3 configuration (uncomment and fill in with your details if needed)
#aws.access_key_id=<your_access_key>
#aws.secret_access_key=<your_secret>
#aws.s3.bucket=<your_bucket>
#aws.s3.region=<bucket_region>

# Database connection settings
spring.datasource.url=jdbc:mysql://localhost:3306/dp1
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# HikariCP settings
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.idle-timeout=10000
# Adjusted max-lifetime to a more reasonable value (30 minutes)
spring.datasource.hikari.max-lifetime=1800000

# Hibernate settings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# JWT Security settings
security.jwt.secret=hsgdasdgaskiwj
security.jwt.issuer=Main
security.jwt.ttlMillis=2928000000

# File upload settings
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB

# CORS configuration
spring.mvc.cors.allowed-origins=http://localhost:3000
spring.mvc.cors.allowed-methods=GET,POST,PUT,DELETE
spring.mvc.cors.allow-credentials=true

# Server settings
server.forward-headers-strategy=FRAMEWORK
server.servlet.context-path=/back

# Removed duplicate and unused properties for clarity and efficiency
